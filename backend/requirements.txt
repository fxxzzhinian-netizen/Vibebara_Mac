# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
websockets==14.1
python-multipart>=0.0.20

# Data validation & settings
pydantic==2.10.3
pydantic-settings==2.7.0

# Database (MySQL async)
sqlalchemy==2.0.36
aiomysql==0.3.2
alembic==1.14.0
# MySQL 8.0 默认 caching_sha2_password 认证需要（aiomysql/PyMySQL 完整认证依赖）
cryptography>=42.0.0

# YAML parsing (native_skill_store)
PyYAML>=6.0

# 滑块人机验证拼图生成（登录/注册）
Pillow>=10.0.0

# File watching (Native Skill Store sync)
watchfiles>=0.24.0

# LLM API client
openai>=1.82.0

# HTTP client（从远程链接 / GitHub 仓库导入 Skill 时下载归档）
httpx>=0.27.0

# 腾讯云 COS 对象存储（STORAGE_BACKEND=cos 时的 Skill 持久化后端）
cos-python-sdk-v5>=1.9.30
